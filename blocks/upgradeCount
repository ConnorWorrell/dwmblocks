#!/bin/dash

case $1 in
	1) notify-send "$(yay -Qu)"; exit ;;
	3) setsid -f "urxvt" -e yay -Su && pkill -RTMIN+6 dwmblocks; exit ;;
esac

image=""

upgrades=$(pacman -Qu | wc | awk '{ print $1 }')

printf "%s %s" "$image" "$upgrades"
